import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
    listItemTitle: {
        fontWeight: 'bold',
        color: '#C7C6CD',
        fontSize: 15,
    },
    listItemSubtitle: {
        color: 'gray'
    }
})